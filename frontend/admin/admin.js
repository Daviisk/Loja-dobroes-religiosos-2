(()=>{
 'use strict';
 const $=s=>document.querySelector(s),cash=c=>(Number(c||0)/100).toLocaleString('pt-BR',{style:'currency',currency:'BRL'}),date=v=>v?new Date(v).toLocaleString('pt-BR'):'—';
 const esc=v=>String(v??'').replace(/[&<>"']/g,ch=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[ch]));
 const labels={pending:'Aguardando',processing:'Processando',paid:'Pago',cancelled:'Cancelado',refunded:'Reembolsado',partially_refunded:'Reembolso parcial',failed:'Falhou',in_mediation:'Mediação',charged_back:'Chargeback',awaiting_payment:'Aguardando pagamento',preparing:'Em preparação',shipped:'Enviado',delivered:'Entregue',hold:'Em análise'};
 const viewTitles={orders:'Pedidos',customers:'Clientes',incidents:'Ocorrências',inventory:'Estoque',audit:'Auditoria',analytics:'Analytics'};
 const api=async(path,options={})=>{const r=await fetch(path,{credentials:'same-origin',cache:'no-store',...options,signal:options.signal||AbortSignal.timeout(15000)});let d=null;try{d=await r.json();}catch{}if(!r.ok)throw Error(d?.message||'Não foi possível concluir.');return d;};
 const loginView=$('#login-view'),appView=$('#app-view'),views=Object.fromEntries([...document.querySelectorAll('.admin-view')].map(v=>[v.id.replace('-view',''),v]));
 let currentView='orders',sessionState={};
 const panelMessage=text=>{$('#panel-message').textContent=text||'';};
 function showApp(){loginView.hidden=true;appView.hidden=false;switchView(currentView);}
 function showLogin(message=''){for(const d of document.querySelectorAll('dialog[open]'))d.close();appView.hidden=true;loginView.hidden=false;$('#login-message').textContent=message;}
 async function session(){try{const s=await api('/api/admin/session');sessionState=s;if(s.authenticated)showApp();else showLogin(s.configured?'':'Configure ADMIN_PASSWORD e ADMIN_SESSION_SECRET no servidor.');}catch{showLogin('Não foi possível abrir o painel.');}}
 $('#login-form').addEventListener('submit',async e=>{e.preventDefault();$('#login-message').textContent='Entrando…';try{await api('/api/admin/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({password:$('#admin-password').value})});$('#admin-password').value='';sessionState=await api('/api/admin/session');showApp();}catch(err){showLogin(err.message);}});
 $('#logout').addEventListener('click',async()=>{try{await api('/api/admin/logout',{method:'POST',headers:{'Content-Type':'application/json'},body:'{}'});}catch{}showLogin();});
 function badge(status){const cls=String(status||'').replace(/[^a-z_]/g,'');return `<span class="badge ${cls}">${esc(labels[status]||status||'—')}</span>`;}
 function copyButton(value,label){return value?`<button type="button" class="copy-mini" data-copy="${encodeURIComponent(String(value))}">Copiar ${esc(label)}</button>`:'';}
 async function copyText(value,button){try{if(navigator.clipboard?.writeText)await navigator.clipboard.writeText(value);else{const area=document.createElement('textarea');area.value=value;area.setAttribute('readonly','');area.style.position='fixed';area.style.opacity='0';document.body.append(area);area.select();document.execCommand('copy');area.remove();}const old=button.textContent;button.textContent='Copiado';setTimeout(()=>{button.textContent=old;},1200);}catch{panelMessage('Não foi possível copiar automaticamente.');}}
 function progress(order){
  const rank={awaiting_payment:0,paid:1,preparing:2,shipped:3,delivered:4};
  const stages=[['Pedido criado','awaiting_payment'],['Pagamento','paid'],['Preparação','preparing'],['Enviado','shipped'],['Entregue','delivered']],current=rank[order.fulfillmentStatus]??0,history=Array.isArray(order.fulfillmentHistory)?order.fulfillmentHistory:[];
  const historyDate=status=>history.findLast?.(item=>item?.to===status)?.at||[...history].reverse().find(item=>item?.to===status)?.at||'';
  const steps=stages.map(([name,status],index)=>{const when=historyDate(status)||(status==='awaiting_payment'?order.createdAt:'');return `<li class="${index<current?'done':index===current?'current':''}"><span>${index+1}</span><strong>${name}</strong>${when?`<small>${esc(date(when))}</small>`:''}</li>`;}).join('');
  const warning=order.fulfillmentStatus==='hold'?'<p class="progress-warning">Pedido em análise. Confira o pagamento antes de continuar a expedição.</p>':order.fulfillmentStatus==='cancelled'?'<p class="progress-warning">Pedido cancelado.</p>':'';
  return `<div class="progress-head"><strong>Andamento do pedido</strong><span>${esc(labels[order.fulfillmentStatus]||order.fulfillmentStatus)}</span></div><ol class="order-progress">${steps}</ol>${warning}`;
 }
 function addressText(a={}){return [a.street&&`${a.street}, ${a.number||'s/n'}${a.complement?' · '+a.complement:''}`,a.district&&`${a.district} · ${a.city||''}/${a.state||''}`,a.cep&&`CEP ${a.cep}`].filter(Boolean).join('\n');}
 function sortOrders(orders){const mode=$('#sort-orders').value,list=[...orders];if(mode==='oldest')return list.sort((a,b)=>Date.parse(a.createdAt)-Date.parse(b.createdAt));if(mode==='highest')return list.sort((a,b)=>Number(b.total||0)-Number(a.total||0));if(mode==='lowest')return list.sort((a,b)=>Number(a.total||0)-Number(b.total||0));return list.sort((a,b)=>Date.parse(b.createdAt)-Date.parse(a.createdAt));}
 async function loadOrders(){
  const q=$('#search').value.trim(),status=$('#payment-filter').value,fulfillment=$('#fulfillment-filter').value,from=$('#date-from').value,to=$('#date-to').value;
  const params=new URLSearchParams({q,status,fulfillment,from,to,limit:'300'});panelMessage('Atualizando pedidos…');
  try{
   const data=await api('/api/admin/orders?'+params);$('#stat-total').textContent=data.summary.total;$('#stat-paid').textContent=data.summary.paid;$('#stat-awaiting-payment').textContent=data.summary.awaitingPayment;$('#stat-awaiting-shipment').textContent=data.summary.awaitingShipment;$('#stat-preparing').textContent=data.summary.preparing;$('#stat-shipped').textContent=data.summary.shipped;$('#stat-revenue').textContent=cash(data.summary.revenuePaid);$('#stat-ticket').textContent=cash(data.summary.ticketAverage);
   const body=$('#orders-body');body.replaceChildren();const orders=sortOrders(data.orders);
   for(const o of orders){const tr=document.createElement('tr'),customer=o.customer||{};tr.innerHTML=`<td><strong>#${esc(o.orderCode)}</strong><span class="muted">${esc(o.paymentId||'sem pagamento')}</span></td><td>${esc(date(o.createdAt))}</td><td><strong>${esc(customer.name||'—')}</strong><span class="muted">${esc(customer.phone||'')}<br>${esc(customer.email||'')}</span></td><td>${(o.items||[]).reduce((n,i)=>n+Number(i.quantity||0),0)}</td><td><strong>${cash(o.total)}</strong></td><td>${badge(o.status)}</td><td>${badge(o.fulfillmentStatus)}</td><td><button class="open-order" data-id="${esc(o.orderId)}">Abrir</button></td>`;body.append(tr);}
   $('#orders-empty').hidden=orders.length>0;body.querySelectorAll('.open-order').forEach(b=>b.addEventListener('click',()=>openOrder(b.dataset.id)));panelMessage(`Atualizado às ${new Date().toLocaleTimeString('pt-BR',{hour:'2-digit',minute:'2-digit'})}.`);
  }catch(err){panelMessage(err.message);if(/login|painel/i.test(err.message))showLogin(err.message);}
 }
 async function loadCustomers(){
  panelMessage('Atualizando clientes…');
  try{const data=await api('/api/admin/customers?'+new URLSearchParams({q:$('#customer-search').value.trim(),limit:'300'}));const body=$('#customers-body');body.replaceChildren();for(const c of data.customers){const tr=document.createElement('tr');tr.innerHTML=`<td><strong>${esc(c.name)}</strong><span class="muted">${esc(c.customerId)}</span></td><td>${esc(c.phone)}<br><span class="muted">${esc(c.email)}</span></td><td>${Number(c.orders||0)}</td><td>${cash(c.totalSpent)}</td><td>${esc(date(c.lastOrderAt))}</td><td><button class="open-customer" data-id="${esc(c.customerId)}">Abrir</button></td>`;body.append(tr);}$('#customers-empty').hidden=data.customers.length>0;body.querySelectorAll('.open-customer').forEach(b=>b.addEventListener('click',()=>openCustomer(b.dataset.id)));panelMessage(`Atualizado às ${new Date().toLocaleTimeString('pt-BR',{hour:'2-digit',minute:'2-digit'})}.`);}catch(err){panelMessage(err.message);if(/login|painel/i.test(err.message))showLogin(err.message);}
 }
 async function loadIncidents(){try{panelMessage('Atualizando ocorrências…');const data=await api('/api/admin/incidents');const body=$('#incidents-body');body.replaceChildren();for(const o of data.orders){const tr=document.createElement('tr');tr.innerHTML=`<td><strong>#${esc(o.orderCode)}</strong><span class="muted">${esc(date(o.createdAt))}</span></td><td>${esc(o.customer?.name||'—')}</td><td>${badge(o.status)}</td><td>${esc(o.providerStatusDetail||'—')}</td><td>${badge(o.fulfillmentStatus)}</td><td><button class="open-order" data-id="${esc(o.orderId)}">Abrir</button></td>`;body.append(tr);}$('#incidents-empty').hidden=data.orders.length>0;body.querySelectorAll('.open-order').forEach(b=>b.addEventListener('click',()=>openOrder(b.dataset.id)));panelMessage(`${data.orders.length} ocorrência(s).`);}catch(err){panelMessage(err.message);}}
 async function loadInventory(){try{panelMessage('Atualizando estoque…');const data=await api('/api/admin/inventory');$('#inventory-mode').textContent=data.enforcementEnabled?'Bloqueio de venda por estoque está ativado.':'Monitoramento disponível, mas o bloqueio de vendas permanece desativado até as quantidades reais serem homologadas.';const body=$('#inventory-body');body.replaceChildren();for(const item of data.items){const tr=document.createElement('tr');tr.dataset.productId=item.productId;tr.innerHTML=`<td><strong>${esc(item.name)}</strong><span class="muted">${esc(item.productId)}</span></td><td><input class="inventory-qty" type="number" min="0" max="1000000" value="${item.quantity??''}" placeholder="Não definido" aria-label="Quantidade de ${esc(item.name)}"></td><td><input class="inventory-min" type="number" min="0" max="1000000" value="${Number(item.minimumQuantity||0)}" aria-label="Estoque mínimo de ${esc(item.name)}"></td><td><label class="toggle-label"><input class="inventory-enabled" type="checkbox" ${item.enabled?'checked':''}> Monitorar</label></td><td>${item.enabled?(item.quantity===null?'<span class="badge processing">Sem quantidade</span>':item.lowStock?'<span class="badge failed">Estoque baixo</span>':'<span class="badge paid">Normal</span>'):'<span class="badge">Desativado</span>'}</td><td><button class="save-inventory secondary">Salvar</button></td>`;body.append(tr);}panelMessage('Estoque atualizado.');}catch(err){panelMessage(err.message);}}
 async function saveInventory(button){const row=button.closest('tr'),productId=row.dataset.productId,raw=row.querySelector('.inventory-qty').value,quantity=raw===''?null:Number(raw),minimumQuantity=Number(row.querySelector('.inventory-min').value||0),enabled=row.querySelector('.inventory-enabled').checked;button.disabled=true;try{await api('/api/admin/inventory/'+encodeURIComponent(productId),{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({quantity,minimumQuantity,enabled})});panelMessage('Estoque salvo.');await loadInventory();}catch(err){panelMessage(err.message);}finally{button.disabled=false;}}
 async function loadAudit(){try{panelMessage('Atualizando auditoria…');const data=await api('/api/admin/audit?limit=300');const body=$('#audit-body');body.replaceChildren();for(const e of data.entries){const tr=document.createElement('tr'),detail=JSON.stringify(e.detail||{});tr.innerHTML=`<td>${esc(date(e.createdAt))}</td><td><strong>${esc(e.action)}</strong></td><td>${esc(e.orderId||'—')}</td><td>${esc(e.actor||'—')}</td><td><span class="audit-detail" title="${esc(detail)}">${esc(detail.length>140?detail.slice(0,140)+'…':detail)}</span></td>`;body.append(tr);}$('#audit-empty').hidden=data.entries.length>0;panelMessage(`${data.entries.length} registro(s) de auditoria.`);}catch(err){panelMessage(err.message);}}
 async function loadAnalytics(){try{panelMessage('Atualizando analytics…');const data=await api('/api/admin/analytics?days=30'),e=data.summary?.events||{};$('#analytics-mode').textContent=data.enabled?'Eventos dos últimos 30 dias. Nenhum dado pessoal é coletado por este módulo.':'Analytics próprio está implementado, mas desativado até ANALYTICS_ENABLED=true e revisão da política de privacidade.';$('#analytics-page').textContent=e.page_view||0;$('#analytics-product').textContent=e.product_view||0;$('#analytics-cart').textContent=e.add_to_cart||0;$('#analytics-shipping').textContent=e.shipping_quote||0;$('#analytics-checkout').textContent=e.checkout_start||0;$('#analytics-payment').textContent=e.payment_start||0;$('#analytics-purchase').textContent=e.purchase||0;panelMessage('Analytics atualizado.');}catch(err){panelMessage(err.message);}}
 async function openCustomer(id){try{const data=await api('/api/admin/customers/'+encodeURIComponent(id)),c=data.customer||{};$('#customer-dialog-title').textContent=c.name||'Cliente';const orders=data.orders||[],addresses=data.addresses||[];$('#customer-detail').innerHTML=`<div class="detail-grid"><section class="detail-card"><h3>Contato</h3><p><strong>${esc(c.name||'—')}</strong></p><p>${esc(c.phone||'—')}<br>${esc(c.email||'—')}</p><div class="copy-row">${copyButton(c.phone,'telefone')}${copyButton(c.email,'e-mail')}</div><p class="muted">Cliente desde ${esc(date(c.createdAt))}</p></section><section class="detail-card"><h3>Resumo</h3><p><strong>${orders.length}</strong> pedido(s)</p><p>Total pago: <strong>${cash(orders.filter(o=>o.status==='paid').reduce((s,o)=>s+Number(o.total||0),0))}</strong></p></section></div><section class="detail-card customer-section"><h3>Endereços utilizados</h3>${addresses.length?addresses.map(a=>`<p>${esc(addressText(a)).replaceAll('\n','<br>')}<br><span class="muted">Último uso: ${esc(date(a.lastUsedAt))}</span></p>`).join(''):'<p class="muted">Nenhum endereço registrado.</p>'}</section><section class="detail-card customer-section"><h3>Histórico de pedidos</h3><div class="table-wrap standalone"><table><thead><tr><th>Pedido</th><th>Data</th><th>Valor</th><th>Pagamento</th><th>Entrega</th><th></th></tr></thead><tbody>${orders.map(o=>`<tr><td>#${esc(o.orderCode)}</td><td>${esc(date(o.createdAt))}</td><td>${cash(o.total)}</td><td>${badge(o.status)}</td><td>${badge(o.fulfillmentStatus)}</td><td><button class="open-order" data-id="${esc(o.orderId)}">Abrir</button></td></tr>`).join('')}</tbody></table></div></section>`;$('#customer-detail').querySelectorAll('.open-order').forEach(b=>b.addEventListener('click',()=>{openOrder(b.dataset.id);}));$('#customer-dialog').showModal();}catch(err){panelMessage(err.message);}}
 async function openOrder(id){
  try{const o=await api('/api/admin/orders/'+encodeURIComponent(id));$('#dialog-title').textContent=`Pedido #${o.orderCode}`;const customer=o.customer||{},a=o.address||{},s=o.shipping||{},address=addressText(a),items=(o.items||[]).map(i=>`<li>${Number(i.quantity||0)} × ${esc(i.name)} — ${cash(i.subtotal)}</li>`).join('');$('#order-detail').innerHTML=`<section id="order-progress-wrap" class="progress-card">${progress(o)}</section><div class="detail-grid"><section class="detail-card"><h3>Cliente</h3><p><strong>${esc(customer.name||'—')}</strong></p><p>${esc(customer.phone||'—')}<br>${esc(customer.email||'—')}</p><div class="copy-row">${copyButton(customer.phone,'telefone')}${copyButton(customer.email,'e-mail')}</div><p class="muted">Cliente: ${esc(customer.customerId||'—')}</p></section><section class="detail-card"><h3>Pagamento</h3><p>${badge(o.status)}</p><p><strong>${cash(o.total)}</strong></p><p class="muted">Payment ID: ${esc(o.paymentId||'—')}<br>${esc(o.providerStatusDetail||'')}</p><div class="copy-row">${copyButton(o.paymentId,'Payment ID')}${copyButton(o.orderCode,'pedido')}</div></section><section class="detail-card"><h3>Entrega</h3><p>${esc(address).replaceAll('\n','<br>')||'—'}</p><p>${esc(s.name||'—')} · ${esc(s.days||'—')} dia(s)</p><div class="copy-row">${copyButton(address,'endereço')}${copyButton(o.trackingCode,'rastreio')}</div></section><section class="detail-card"><h3>Itens</h3><ul>${items}</ul><p>Produtos: ${cash(o.productsTotal)}<br>Frete: ${cash(s.priceCents)}<br><strong>Total: ${cash(o.total)}</strong></p></section></div><form id="fulfillment-form" class="detail-form"><label>Status da entrega<select id="fulfillment-status"><option value="awaiting_payment">Aguardando pagamento</option><option value="paid">Pago</option><option value="preparing">Em preparação</option><option value="shipped">Enviado</option><option value="delivered">Entregue</option><option value="hold">Em análise</option><option value="cancelled">Cancelado</option></select></label><label>Código de rastreio<input id="tracking-code" maxlength="100" value="${esc(o.trackingCode||'')}"></label><label>Observações<textarea id="admin-notes" maxlength="1000"></textarea></label><div class="form-actions"><button type="submit">Salvar alterações</button>${sessionState.emailNotificationsConfigured?'<button type="button" id="retry-notifications" class="secondary">Reprocessar e-mails</button>':''}</div><p id="detail-message" class="message" role="status"></p></form>`;$('#fulfillment-status').value=o.fulfillmentStatus||'awaiting_payment';$('#admin-notes').value=o.adminNotes||'';$('#fulfillment-form').addEventListener('submit',async e=>{e.preventDefault();const button=e.currentTarget.querySelector('button[type="submit"]');button.disabled=true;$('#detail-message').textContent='Salvando…';try{const saved=await api('/api/admin/orders/'+encodeURIComponent(id),{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({fulfillmentStatus:$('#fulfillment-status').value,trackingCode:$('#tracking-code').value,adminNotes:$('#admin-notes').value})});$('#detail-message').textContent='Alterações salvas.';$('#order-progress-wrap').innerHTML=progress(saved);loadOrders();}catch(err){$('#detail-message').textContent=err.message;}finally{button.disabled=false;}});$('#retry-notifications')?.addEventListener('click',async e=>{e.currentTarget.disabled=true;try{const result=await api('/api/admin/notifications/'+encodeURIComponent(id)+'/retry',{method:'POST',headers:{'Content-Type':'application/json'},body:'{}'});$('#detail-message').textContent=result.configured?`${result.processed} notificação(ões) processada(s).`:'Notificações por e-mail não estão configuradas.';}catch(err){$('#detail-message').textContent=err.message;}finally{e.currentTarget.disabled=false;}});$('#order-dialog').showModal();}catch(err){panelMessage(err.message);if(/login|painel/i.test(err.message))showLogin(err.message);}
 }
 $('#order-detail').addEventListener('click',e=>{const button=e.target.closest('[data-copy]');if(button)copyText(decodeURIComponent(button.dataset.copy),button);});$('#customer-detail').addEventListener('click',e=>{const button=e.target.closest('[data-copy]');if(button)copyText(decodeURIComponent(button.dataset.copy),button);});
 $('#inventory-body').addEventListener('click',e=>{const button=e.target.closest('.save-inventory');if(button)saveInventory(button);});
 $('#dialog-close').addEventListener('click',()=>$('#order-dialog').close());$('#customer-dialog-close').addEventListener('click',()=>$('#customer-dialog').close());
 async function loadCurrent(){if(currentView==='orders')return loadOrders();if(currentView==='customers')return loadCustomers();if(currentView==='incidents')return loadIncidents();if(currentView==='inventory')return loadInventory();if(currentView==='audit')return loadAudit();if(currentView==='analytics')return loadAnalytics();}
 function switchView(name){currentView=name;for(const [key,view] of Object.entries(views))view.hidden=key!==name;document.querySelectorAll('.nav').forEach(n=>n.classList.toggle('active',n.dataset.view===name));$('#page-title').textContent=viewTitles[name]||name;loadCurrent();}
 $('#refresh').addEventListener('click',loadCurrent);document.querySelectorAll('.nav').forEach(btn=>btn.addEventListener('click',()=>switchView(btn.dataset.view)));
 let timer;$('#search').addEventListener('input',()=>{clearTimeout(timer);timer=setTimeout(loadOrders,250);});for(const id of ['payment-filter','fulfillment-filter','date-from','date-to','sort-orders'])$('#'+id).addEventListener('change',loadOrders);$('#customer-search').addEventListener('input',()=>{clearTimeout(timer);timer=setTimeout(loadCustomers,250);});$('#clear-filters').addEventListener('click',()=>{for(const id of ['search','payment-filter','fulfillment-filter','date-from','date-to'])$('#'+id).value='';$('#sort-orders').value='newest';loadOrders();});
 $('#export-backup').addEventListener('click',async()=>{const button=$('#export-backup');button.disabled=true;panelMessage('Gerando backup…');try{const response=await fetch('/api/admin/export',{credentials:'same-origin',cache:'no-store',signal:AbortSignal.timeout(20000)});if(!response.ok)throw Error('Não foi possível exportar os dados.');const blob=await response.blob(),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download=`dobroes-backup-${new Date().toISOString().slice(0,10)}.json`;document.body.append(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(url),1000);panelMessage('Backup exportado.');}catch(err){panelMessage(err.message);}finally{button.disabled=false;}});
 session();
})();
